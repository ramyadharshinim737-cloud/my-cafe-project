

services:
  backend:
    build: ./backend
    ports:
      - "5000:5000"
    environment:
      - MONGODB_URI=mongodb+srv://admin:cafe123@cluster0.nfyxkt9.mongodb.net/cafe_db

  frontend:
    build: ./frontend
    ports:
      - "8080:80"
    depends_on:
      - backend